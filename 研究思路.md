# 研究思路整理

## 黄河三角洲特定要素的解译标志构建
1.解译标志可细分为自然岸线与人工岸线两大类（这个不清楚怎么划分）

3.数据图两个问题：（1）右侧有拼接缝，真彩色下很淡，假彩色下很明显的色差
（2）最后裁剪后周围有无效黑边

## 模型改进
- **主模型**: U-Net++和ResNet50(ImageNet)
- **引入注意力机制与多尺度感知**: CA；ASPP；DCNv2
- **损失函数**: 引入 Sobel-Edge Loss，采用加权组合的方式构建总损失函数Focal Loss + Dice Loss + Sobel-Edge Loss
- **优化器**: AdamW
- **学习率**: 余弦退火算法Warmup（预热） + Cosine Annealing（余弦退火）
- **精度评估**：（1）IoU、Kappa 系数、F1 分数（2）均方根误差 (RMSE)：在提取出的岸线上随机选取验证点，逐点计算其到参考岸线的欧氏距离 。

## 训练调参
1.输入块：256×256（数据图多出时，每次是随机抽取哪个不要 ，保证精度）

2.数据增强：旋转翻转（这里面代码是随机生成的，每一次看到的方向都不一样，这样既避免了数据量过大且保证了效果）

3.epoch：epoch早停机制.容忍度(Patience)：15；最小变化量 (Min Delta):

4.Batch Size:16 （不断改成大的，得出最大的参数，这个参数主要是看显卡的程度）

5.学习率lr:1e-4

6.混合损失函数的权重配比：

## 调参过程


## 训练中的问题


## 预测部分问题
- **编写形态学与连通性分析代码**：
- **边缘追踪与导出**：

## 做消融实验

## 黄河三角洲海岸线动态演变与驱动力深度分析
1.数字岸线分析系统 (DSAS) 的应用
